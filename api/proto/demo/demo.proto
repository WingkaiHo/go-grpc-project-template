syntax = "proto3";
package demo;

service Details {
    rpc Get (GetDetailRequest) returns (Detail);
}

message GetDetailRequest {
    uint64 id = 1;
}

message Detail {
    uint64 id = 1;
    string name = 2;
}